#!/usr/bin/python3

def max_integer(my_list=[]):

    # check empty LIST
    if not my_list:
        return None

    B_int = my_list[0]
    # find max integer
    for num in my_list:
        if num > big_int:
            B_int = num

    return big_int
